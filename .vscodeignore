# Allowlist: exclude everything, then add back exactly what ships. Keeps anything
# new in the working tree out of the package by default.
**
!LICENSE
!README.md
!CHANGELOG.md
!package.json
!icon.png
!out/decide.js
!out/extension.js
